{
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w & npx webpack & npx live-server",
    "start": "tsc && npx webpack && && npx live-server"
  },
  "devDependencies": {
    "@types/google.maps": "^3.45.6",
    "live-server": "^1.2.1",
    "ts-loader": "^9.2.5",
    "typescript": "^4.4.2",
    "webpack": "^5.51.1",
    "webpack-cli": "^4.8.0"
  }
}